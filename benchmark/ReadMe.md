# Benchmark

**NOTE**: To run the experience, please comment out the caching mechanism in the code generator.



Please run the following command in the console to get the time using the single-function approach and the im2im for any pair of conversion in the graph.  The `2028` and `3` can be replace by other parameters.

```cmd
python measure_time_v2.py --res 2048 --repeat 3
```

